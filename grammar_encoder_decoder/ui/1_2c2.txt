from glob import glob
from os.path import basename, splitext
models = glob("models/*.param")
model_num = 0
for model in models:
	print str(model_num) + ": " + splitext(basename(model))[0]
	model_num += 1
model_num = raw_input("Choose a model number: ")

model_name = basename(models[int(model_num)])
model_file = open(models[int(model_num)])
exec(model_file)
