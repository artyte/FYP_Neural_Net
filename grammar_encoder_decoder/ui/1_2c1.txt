hyperparam_file = open(join("data","hyperparam_defaults.txt"))
hyperparams = hyperparam_file.readlines()
hyperparam_file.close()

import re
for i, hyperparam in enumerate(hyperparams):
	hyperparam = re.sub('\n', '', hyperparam)
	hyperparameters[hyperparam] = data[i]
